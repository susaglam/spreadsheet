- Filter revision commands of OCA dashboards (e.g. a domain changed after the last
  snapshot) the same way as the snapshot data sources.
- Rules for non-dashboard spreadsheets.
- Show a notice to dashboard viewers when a rule fails closed.
